tags_filepath: /home/daslab/Documents/repositories/pose_estimation/src/tagslam/data/apriltags_apt5.csv
output_body_poses_path: /home/daslab/Documents/repositories/pose_estimation/src/tagslam/results/body_poses.txt
vio_topic: /zed2i/zed_node/odom
output_topic: /tagslam/body_pose
global_frame_id: map
body_frame_id: zed_baselink
noise_models:
  # Uncertainty of initial body pose computed by PNP [rad, rad, rad, m, m, m]
  initial_pose_noise: [0.1, 0.1, 0.1, 0.05, 0.05, 0.05]
  # Uncertainty of known apriltags position[m, m, m]
  apriltags_noise: [0.02, 0.02, 0.02]
  # Uncertainty of delta-vio measurements [rad, rad, rad, m, m, m]
  relative_pose_noise: [0.1, 0.1, 0.1, 0.05, 0.05, 0.05]
  cam_noise: 1
  
isam2:
  relinearizeThreshold: 0.01
  relinearizeSkip: 1
  reset_after_time: 5.0
